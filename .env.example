# Port the API server listens on
PORT=3000

# Comma-separated list of allowed CORS origins ("*" allows all — fine for local testing)
CORS_ORIGIN=*

# Rate limit: max requests per IP per window
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=30
